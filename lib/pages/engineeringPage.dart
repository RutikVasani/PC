import 'package:flutter/material.dart';

class EngineeringPage extends StatelessWidget {
  const EngineeringPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(child: Text('Engineering page')),
    );
  }
}
