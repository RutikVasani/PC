import 'package:flutter/material.dart';

class allPc extends StatelessWidget {
  const allPc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Material(
      child: Center(child: Text("ALL Pc")),
    );
  }
}
